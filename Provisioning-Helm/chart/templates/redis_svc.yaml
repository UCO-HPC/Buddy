apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.redis.name }}-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
    - port: {{ .Values.redis.port }}
  selector:
    app: {{ .Release.Name }}
    tier: cache